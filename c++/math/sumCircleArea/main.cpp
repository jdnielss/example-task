#include <iostream>

using namespace std;

int main() {
    double side = 3.5;
    double circumference = (side * 2) * 2;
    double area = side * side;

    cout << "Side : " << circumference << endl;
    cout << "Area : " << area;

    return 0;
};
